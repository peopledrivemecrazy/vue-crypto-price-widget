{
  "name": "vue-crypto-price-widget-v2",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "serve dist",
    "clean": "yarn build && yarn start",
    "deploy": "node scripts/gh-pages-deploy.js"
  },
  "dependencies": {
    "vue": "^3.0.2"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.2",
    "execa": "^4.1.0",
    "vite": "^1.0.0-rc.8"
  }
}
